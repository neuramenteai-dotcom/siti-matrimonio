# Siti matrimonio

Vetrina (`index.html`) e configuratore con calcolo del prezzo (`landing-configuratore.html`).
Si modificano nella cartella `vendita/` del progetto e si ripubblicano con `pubblica.py`.
